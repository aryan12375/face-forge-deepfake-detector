# Copy this to .env.local for local overrides
# Vite exposes variables prefixed with VITE_ to the browser

# Override the API base URL (leave blank to use Vite proxy in dev)
# In production point this at your deployed backend
VITE_API_BASE=
