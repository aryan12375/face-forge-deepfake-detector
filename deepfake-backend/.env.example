# Copy this to .env and fill in values
# The app reads these via os.environ

# Path to your fine-tuned model checkpoint
# Produced by: python scripts/train.py --output_dir ./checkpoints
MODEL_CHECKPOINT=checkpoints/best_model.pth

# Server settings (used by run.py)
HOST=0.0.0.0
PORT=8000
